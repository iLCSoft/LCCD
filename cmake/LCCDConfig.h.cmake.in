#ifndef LCCDConfig_h
#define LCCDConfig_h 1

// version macros
#define LCCD_MAJOR_VERSION @LCCD_VERSION_MAJOR@
#define LCCD_MINOR_VERSION @LCCD_VERSION_MINOR@
#define LCCD_PATCH_LEVEL @LCCD_VERSION_PATCH@

//#define LCCD_VERSION_GE( MAJV , MINV , PLEV )  ( (LCCD_MAJOR_VERSION > MAJV) || ( (LCCD_MAJOR_VERSION == MAJV) && (LCCD_MINOR_VERSION > MINV) ) || ( (LCCD_MAJOR_VERSION == MAJV) && (LCCD_MINOR_VERSION == MINV) && (LCCD_PATCH_LEVEL >= PLEV) ) )
#define LCCD_VERSION_GE( MAJV , MINV )  ( (LCCD_MAJOR_VERSION > MAJV) || ( (LCCD_MAJOR_VERSION == MAJV) && (LCCD_MINOR_VERSION >= MINV) ) )

#cmakedefine LCCD_CONDDB

#endif // LCCDConfig_h
